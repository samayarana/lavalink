# ----------------------------------------------------------------------
# I. CẤU HÌNH SERVER VÀ PORT (BẮT BUỘC CHO RENDER)
# ----------------------------------------------------------------------
server:
  port: 80 # 🌟 Cổng bắt buộc cho Render
  address: 0.0.0.0
  http2:
    enabled: false

# ----------------------------------------------------------------------
# II. KHAI BÁO CÁC PLUGIN BÊN NGOÀI
# ----------------------------------------------------------------------
plugins:
  # ⚠️ CHỈ SỬ DỤNG LAVASRC (LAVA MEDIA SOURCE)
  - dependency: "dev.arbjerg.lavalink.plugins.lavasrc.LavaSrc" 
    
# ----------------------------------------------------------------------
# III. CẤU HÌNH LAVALINK CHÍNH (lavalink:)
# ----------------------------------------------------------------------
lavalink:
  
  # A. CẤU HÌNH CHO LAVASRC PLUGIN
  plugins:
    lavasrc:
      spotify:
        clientId: "85aab1d51a174aad9eed6d7989f530e6" # Thay bằng Client ID thật
        clientSecret: "b2ad05aa725e434c88776a1be8eab6c2" # Thay bằng Client Secret thật
        countryCode: "IN"

  # B. CẤU HÌNH SERVER CHUNG
  server:
    password: "e" # Mật khẩu node của bạn
    
    # ⚠️ CÁC NGUỒN TÌM KIẾM
    sources:
      # TẮT NGUỒN YOUTUBE MẶC ĐỊNH ĐỂ LAVASRC XỬ LÝ
      youtube: false 
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      nico: true
      http: true 
      local: false

    # CÁC THIẾT LẬP KHÁC (GIỮ NGUYÊN)
    filters:
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    nonAllocatingFrameBuffer: false
    bufferDurationMs: 400
    frameBufferDurationMs: 5000
    opusEncodingQuality: 10
    resamplingQuality: LOW 
    trackStuckThresholdMs: 10000
    useSeekGhosting: true
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5
    youtubeSearchEnabled: false # Lavalink v4.x tự động quản lý qua LavaSrc
    soundcloudSearchEnabled: true
    gc-warnings: true
    timeouts:
      connectTimeoutMs: 3000
      connectionRequestTimeoutMs: 3000
      socketTimeoutMs: 3000

# ----------------------------------------------------------------------
# IV. CẤU HÌNH LOGGING VÀ METRICS (KHÔNG CẦN CHỈNH SỬA)
# ----------------------------------------------------------------------
metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""

logging:
  file:
    path: ./logs/
  level:
    root: INFO
    lavalink: INFO
  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000
  logback:
    rollingpolicy:
      max-file-size: 1GB
      max-history: 30
