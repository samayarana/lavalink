# CẤU HÌNH CHO SERVER VÀ PORT
server: # REST and WS server
  port: 80 # 🌟 ĐÃ CHỈNH SỬA: Cổng BẮT BUỘC cho Render Web Service Free/Starter
  address: 0.0.0.0
  http2:
    enabled: false

# 🛠️ CẤU HÌNH PLUGIN: BẮT BUỘC CHO LAVALINK V4.X
plugins:
  # 1. Plugin YouTube chính thức (Quan trọng nhất)
  - dependency: "dev.lavalink.youtube:youtube-source:1.4.0" # Kiểm tra phiên bản mới nhất nếu cần

  # 2. Plugin Spotify (Cần Client ID/Secret)
  - dependency: "dev.lavalink.spotify:spotify-source:1.4.0"

  # 3. Plugin Apple Music (Tùy chọn)
  - dependency: "dev.lavalink.apple:apple-music-source:1.4.0"

# lavalink: (Cấu hình chính của Lavalink)
lavalink:
  plugins:
    # (Để trống hoặc comment các mục ở đây)

  server:
    password: "iamprmgvyt"
    sources:
      # YouTube mặc định PHẢI TẮT vì ta đã dùng plugin ở trên
      youtube: true
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      nico: true
      http: true 
      local: false
    
    # 🔑 CẤU HÌNH SPOTIFY: BẮT BUỘC PHẢI CÓ
    spotify:
      clientId: "85aab1d51a174aad9eed6d7989f530e6" # Lấy từ Spotify Developer Dashboard
      clientSecret: "b2ad05aa725e434c88776a1be8eab6c2" # Lấy từ Spotify Developer Dashboard
      countryCode: "VN" # Mã quốc gia

    # 🍎 CẤU HÌNH APPLE MUSIC: TÙY CHỌN
    # applemusic:
    #   mediaAPIToken: "⚠️ APPLE_MUSIC_DEVELOPER_TOKEN" # Cần token nếu muốn dùng

    # CÁC THIẾT LẬP KHÁC (GIỮ NGUYÊN HOẶC CHỈNH SỬA TÙY Ý)
    filters:
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    nonAllocatingFrameBuffer: false
    bufferDurationMs: 400
    frameBufferDurationMs: 5000
    opusEncodingQuality: 10
    resamplingQuality: LOW 
    trackStuckThresholdMs: 10000
    useSeekGhosting: true
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true
    gc-warnings: true
    timeouts:
      connectTimeoutMs: 3000
      connectionRequestTimeoutMs: 3000
      socketTimeoutMs: 3000

# THIẾT LẬP METRICS VÀ LOGGING (THƯỜNG KHÔNG CẦN CHỈNH SỬA)
metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""

logging:
  file:
    path: ./logs/
  level:
    root: INFO
    lavalink: INFO
  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000
  logback:
    rollingpolicy:
      max-file-size: 1GB
      max-history: 30
