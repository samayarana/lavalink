# ----------------------------------------------------------------------
# I. CẤU HÌNH SERVER VÀ PORT (BẮT BUỘC CHO RENDER)
# ----------------------------------------------------------------------
server:
  port: 80 # 🌟 Đã sửa: Cổng bắt buộc cho Render
  address: 0.0.0.0
  http2:
    enabled: false

# ----------------------------------------------------------------------
# II. KHAI BÁO CÁC PLUGIN BÊN NGOÀI
# ----------------------------------------------------------------------
# Lưu ý: Các plugin này phải có trong thư mục plugins/ hoặc được tải qua Maven/Gradle
plugins:
  # 1. Plugin YouTube chính thức (Quan trọng nhất)
  - dependency: "dev.lavalink.youtube:youtube-source:1.4.0" 

  # 2. Plugin Spotify (Cần Client ID/Secret)
  - dependency: "dev.lavalink.spotify:spotify-source:1.4.0"

  # 3. Plugin Apple Music (Tùy chọn)
  - dependency: "dev.lavalink.apple:apple-music-source:1.4.0"
  
# ----------------------------------------------------------------------
# III. CẤU HÌNH LAVALINK CHÍNH (lavalink:)
# ----------------------------------------------------------------------
lavalink:
  
  # A. CẤU HÌNH CHO CÁC PLUGIN ĐÃ KHAI BÁO Ở TRÊN
  plugins:
    # Cấu hình Plugin YouTube Source: Cho phép tìm kiếm
    youtube:
      allowSearch: true 
    
    # Cấu hình Plugin Spotify Source: Sử dụng thông tin đăng nhập của bạn
    spotify:
      clientId: "85aab1d51a174aad9eed6d7989f530e6" # Thay bằng Client ID thật
      clientSecret: "b2ad05aa725e434c88776a1be8eab6c2" # Thay bằng Client Secret thật
      countryCode: "VN"

    # Cấu hình Plugin Apple Music Source (nếu cần)
    # applemusic:
    #   mediaAPIToken: "⚠️ APPLE_MUSIC_DEVELOPER_TOKEN"
      
  # B. CẤU HÌNH SERVER CHUNG
  server:
    password: "iamprmgvyt" # Mật khẩu node của bạn
    
    # Kích hoạt các nguồn tìm kiếm:
    sources:
      # 🌟 ĐÃ SỬA: KÍCH HOẠT LẠI NGUỒN YOUTUBE MẶC DỤ ĐÃ DÙNG PLUGIN
      youtube: true 
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      nico: true
      http: true 
      local: false

    # Cấu hình tìm kiếm mặc định (Sử dụng các Plugin đã bật):
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true
    
    # Các thiết lập khác (giữ nguyên):
    filters:
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    nonAllocatingFrameBuffer: false
    bufferDurationMs: 400
    frameBufferDurationMs: 5000
    opusEncodingQuality: 10
    resamplingQuality: LOW 
    trackStuckThresholdMs: 10000
    useSeekGhosting: true
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5
    gc-warnings: true
    timeouts:
      connectTimeoutMs: 3000
      connectionRequestTimeoutMs: 3000
      socketTimeoutMs: 3000

# ----------------------------------------------------------------------
# IV. CẤU HÌNH LOGGING VÀ METRICS (KHÔNG CẦN CHỈNH SỬA)
# ----------------------------------------------------------------------
metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""

logging:
  file:
    path: ./logs/
  level:
    root: INFO
    lavalink: INFO
  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000
  logback:
    rollingpolicy:
      max-file-size: 1GB
      max-history: 30
